import ContactsContainer from "../../components/ContactsContainer/ContactsContainer.jsx";

export const metadata = {
  title: "Contacts | Mont Blanc",
  description: "Contact information, store address, phone numbers and map for Mont Blanc.",
};

export default function ContactsPage() {
  return <ContactsContainer />;
}
